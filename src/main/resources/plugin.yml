name: Split
version: '${project.version}'
main: com.siberanka.split.SplitPlugin
api-version: '1.20'
folia-supported: true
description: Dynamic platform, expression, switch, and adaptive output placeholders.
author: SiberAnka
website: https://siberanka.com
softdepend:
  - Floodgate
  - PlaceholderAPI
commands:
  split:
    description: Split plugin command.
    permission: split.admin
    usage: /split [reload]
